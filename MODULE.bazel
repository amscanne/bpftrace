module(
    name = "bpftrace",
    version = "0.23.0",
)

# Core Bazel rules
bazel_dep(name = "rules_cc", version = "0.0.9")
bazel_dep(name = "platforms", version = "0.0.10")

# Flex and Bison for parser generation
bazel_dep(name = "rules_flex", version = "0.2.1")
bazel_dep(name = "rules_bison", version = "0.2.2")

# LLVM/Clang dependencies
bazel_dep(name = "llvm-project", version = "18.1.8")

# System dependencies (these would typically be provided by the system)
# Note: These are placeholders - actual system dependencies would need
# to be configured based on your specific setup

# For zlib
bazel_dep(name = "zlib", version = "1.3.1")

# Additional system libraries that would need to be configured:
# - libbpf (BPF library)
# - libbcc (BCC library)
# - libelf (ELF library)
# - libdw (DWARF library)
# - libbfd (BFD library)
# - libopcodes (opcodes library)
# - libpcap (packet capture library)
# - libcereal (serialization library)

# These system dependencies would typically be handled through:
# 1. System package manager integration
# 2. Custom repository rules
# 3. Vendored dependencies
# 4. Conan integration

# Example of how you might handle system dependencies:
# You would need to create repository rules or use existing ones
# to find and configure these system libraries.

# For development, you might want to add:
# bazel_dep(name = "googletest", version = "1.14.0", dev_dependency = True)
# bazel_dep(name = "google_benchmark", version = "1.8.3", dev_dependency = True)
