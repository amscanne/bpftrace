load("//:build_defs.bzl", "cc_library")

package(default_visibility = ["//visibility:public"])

cc_library(
    name = "stdlib",
    srcs = glob(["*.cpp"]),
    hdrs = glob(["*.h"]),
    includes = [".."],
)
